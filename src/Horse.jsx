const Horse = ({x, y, color, width, height}) =>
	<div style={{backgroundColor: color, top: y, left: x, width, height, borderRadius: '40px', position: 'absolute'}}></div>;

const generateHorses = () => {};
export default Horse;
